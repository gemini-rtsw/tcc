To install the tcc:

cd /gemini/tcc
cvs checkout -r <version> -d <version> tcc

where <version> is the version you want (e.g. V1-6)

cd <version>
gmake MK (or gmake CP)
cd ..
rm tcc
ln -s <version> tcc
