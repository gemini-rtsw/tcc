#+
#  SystemBeep.itcl
#
#  Command to produce an audible "beep"
#
#+

proc systemBeep {} {
   if { [exec uname] ne "Linux" } {
      bell
   } else {
      puts stdout \a
      #catch { exec echo -e "\a" &}
      #catch { exec aplay [ file join $::ROOT lib/alert.wav ] 2>/dev/null &}
   }
}
